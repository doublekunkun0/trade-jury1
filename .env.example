# Optional: configure OKX DEX API credentials for preferred live quote source.
# If these are absent or the OKX host is unreachable, the demo will fall back
# to a public live quote provider while keeping the same jury logic.

OKX_DEX_API_KEY=
OKX_DEX_SECRET_KEY=
OKX_DEX_PASSPHRASE=
